---
import { getCourses } from "../lib/notion.js";
const courses = await getCourses();
---

<html>
  <head><title>课程列表</title></head>
  <body>
    <h1>课程列表</h1>
    <ul>
      {courses.map(course => (
        <li>
          <a href={`/courses/${course.slug}`}>{course.title}</a>
          <span>类别：{course.category} 价格：￥{course.price}</span>
        </li>
      ))}
    </ul>
  </body>
</html>